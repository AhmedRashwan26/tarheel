# 📌 سجل إنجازات ومهام منصة ترحيل (Tarheel Project Roadmap & Punch List)

تاريخ التحديث: 05 سبتمبر 2026

---

## أولاً: ما تم إنجازه وحفظه اليوم (Completed & Saved Today)

1. **تحديث وتجهيز نماذج قاعدة البيانات (Prisma Schema):**
   - إضافة `PAYONEER` إلى قائمة وسائل الدفع (`PaymentMethod`).
   - تحديث نموذج `EscrowPayment` لدعم جلسات الدفع الإلكتروني (`payoneerSessionId` و `payoneerLongId`).
   - التحقق من توافق العلاقات المالية والضمان في قاعدة البيانات.

2. **توثيق وتخطيط تكامل بوابة Payoneer Checkout:**
   - توثيق الروابط الرسمية لبوابة بايونير وشروط إنشاء الحساب التجاري للتجار.
   - وضع خطة الربط البرمجي الكاملة لإنشاء جلسات الدفع واستقبال الـ Webhooks لتفعيل الضمان المالي تلقائياً.

3. **مراجعة وتدقيق نظام المحادثات المباشرة (In-App Chat):**
   - فحص ربط الباك إند (NestJS) مع تطبيق الموبايل (Flutter) في تبادل الرسائل النصية والصوتية والمواقع الجغرافية بين العميل والسائق.

4. **حفظ حالة المشروع برمجياً (Git State):**
   - حفظ كافة التعديلات والتأكد من سلامة ملفات المشروع واستقرار الكود.

---

## ثانياً: قائمة المهام المتبقية للإطلاق الرسمي (Upcoming Launch Tasks)

### 1. 💳 التكامل الكامل مع بوابة الدفع ببايونير (Payoneer Checkout Integration)
- [ ] إدخال مفاتيح الاعتماد الحقيقية (`Store Code`, `API Username`, `API Password`) في ملف `.env` بعد استلامها من بايونير.
- [ ] بناء خدمة `PayoneerService` لتوليد جلسات الدفع الإلكتروني (`POST /payments/payoneer/checkout-session`).
- [ ] تفعيل مسار الويب هوك المباشر (`POST /payments/payoneer/webhook`) لتأكيد الدفع وحجز المبلغ تلقائياً في ضمان ترحيل وتحديث رصيد السائق المعلق وإشعاره.

### 2. 📱 التعديلات والتحسينات على الفرونت إند (Frontend Flutter App)
- [ ] ربط شاشة الدفع والضمان (`escrow_checkout_screen.dart`) بنافذة Payoneer Checkout المباشرة.
- [ ] مراجعة تدفقات واجهات المستخدم (UI/UX) لكلا الطرفين (العميل والسائق).
- [ ] تحسين سرعة وسلاسة التنقل ومطابقة الهوية البصرية.

### 3. 🌐 استخدام رقم واتساب دولي عبر e-SIM (بديلاً عن الرقم المصري)
- [ ] شراء شريحة إلكترونية دولية (e-SIM) من أي دولة (مثل رقم بريطاني، أمريكي، أو خليجي).
- [ ] تسجيل الرقم في تطبيق واتساب الأعمال (WhatsApp Business) وربطه ببوابة الرسائل (`whatsapp-gateway`).
- [ ] ضمان استقرار إرسال رموز التحقق (OTP) والإشعارات المباشرة للمستخدمين دون انقطاع أو حظر.

### 4. 🧹 حذف جميع البيانات الافتراضية بالباك إند (Production Database Purge)
- [ ] إعداد سكربت تصفير وتنظيف قاعدة البيانات من جميع السجلات التجريبية (Fake Users, Mock Trips, Dummy Contracts).
- [ ] التأكد من تجهيز جداول الإنتاج النظيفة مع إبقاء حسابات الإدارة الرسمية والبيانات الأساسية فقط تمهيداً للإطلاق.

### 5. ⚙️ تعديل نسبة عمولة التطبيق إلى 13.50% وإدخال المميزات الجديدة
- [x] تعديل نسبة العمولة في كامل المنصة والباك إند وتطبيق الموبايل من 10% إلى **13.50%**:
  - تحديث الثوابت البرمجية `PLATFORM_CONSTANTS.COMMISSION_PERCENTAGE = 13.50` و `DRIVER_PERCENTAGE = 86.50`.
  - تحديث القيمة الافتراضية في `schema.prisma` (`platformCommissionRate Float @default(13.5)`).
  - تحديث الحسابات البرمجية وفواتير العقود في `contracts.service.ts` و `payments.service.ts` و `offers.service.ts` و `admin.service.ts` لتوزيع (13.50% للمنصة و 86.50% للسائق).
  - تحديث واجهات وتنبيهات وحسابات تطبيق فلاتر (`driver_feed_screen.dart`, `driver_main_screen.dart`, `terms_and_conditions_screen.dart`, `driver_register_screen.dart`).
  - تحديث اختبارات الوحدة Jest وتمريرها بنجاح.
- [ ] إضافة المميزات الجديدة المقررة للمنصة قبل الطرح في المتاجر.
